apiVersion: v1
kind: Service
metadata:
  name: go-app
  namespace: apps
  labels:
    app: go-app
spec:
  type: ClusterIP
  selector:
    app: go-app
  ports:
    - name: http
      port: 8080
      targetPort: http
